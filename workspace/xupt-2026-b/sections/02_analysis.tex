\section{问题分析}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{figures/fig_method_flowchart.png}
    \caption{本文方法论流程：四问递进结构与数据流}
    \label{fig:method_flow}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{figures/fig_noise_heatmap.png}
    \caption{三份附件噪声水平热力图（二阶差分估计 $\sigma$，单位 m）}
    \label{fig:noise_heat}
\end{figure}

\subsection{问题一的分析}
问题一本质是\textbf{一维参数估计}问题：两路无噪声轨迹完全相同，仅存在时间轴平移 $\Delta\tau$。由于无噪声，残差平方和在真值处为零，可利用三次样条保持轨迹光滑性后做高精度一维搜索。

\subsection{问题二的分析}
问题二在时间偏差基础上新增\textbf{空间系统偏差}和\textbf{随机噪声}，变为三参数联合估计 + 状态滤波问题。先用最小二乘联合估出 $(\Delta\tau, \Delta x, \Delta y)$，再用卡尔曼滤波抑制噪声输出平滑轨迹。

\subsection{问题三的分析}
问题三的核心是\textbf{模型选择}：真实数据是否存在系统偏差？这是假设检验问题（H$_0$: 无偏 vs H$_1$: 有偏）。采用 Wald + AIC 双判据提高鲁棒性。

\subsection{问题四的分析}
问题四是带约束的\textbf{组合优化}：在固定轨迹上选择时刻执行任务，约束包括距离/速度/加速度硬阈值、准备时长窗口、角差限制和时序不冲突。难点在于噪声轨迹微分放大，需先平滑再优化。

\subsection{数据特征}

图~\ref{fig:eda_traj} 展示了三份附件的轨迹分布。附件1/2 公共时段充分（$>500$\,s），附件3两路时段完全不重叠（方式1 $t\in[469, 814]$\,s，方式2 $t\in[77, 401]$\,s），表明时间偏差 $\Delta\tau \approx 368$\,s。

\begin{figure}[H]
    \centering
    \includegraphics[width=0.48\textwidth]{figures/eda_a1_trajectory.png}
    \includegraphics[width=0.48\textwidth]{figures/eda_a3_trajectory.png}
    \caption{附件1（左，无噪声）与附件3（右，真实数据）轨迹对比}
    \label{fig:eda_traj}
\end{figure}

\subsection{整体求解思路}
本文整体求解思路如图~\ref{fig:framework} 所示。

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[node distance=0.9cm and 1.5cm]
        \node[terminal]                              (p)   {题目分析};
        \node[procbox,  below=of p]                  (a)   {模型假设};
        \node[procbox,  below=of a]                  (n)   {符号说明};
        \node[procbox,  below=of n]                  (m1)  {问题一：样条 + 一维搜索};
        \node[procbox,  below=of m1]                 (m2)  {问题二：联合估计 + KF};
        \node[procbox,  below=of m2]                 (m3)  {问题三：Wald + AIC 检验};
        \node[procbox,  below=of m3]                 (m4)  {问题四：SG 平滑 + 贪心优化};
        \node[decision, below=of m4, yshift=-0.3cm]  (d)   {灵敏度\\检验通过？};
        \node[terminal, below=of d, yshift=-0.3cm]   (c)   {结论与评价};

        \node[draw=black, dashed, thick,
              fit=(m1)(m2)(m3)(m4),
              inner sep=10pt,
              label={[font=\small\heiti]right:{核心建模}}] (core) {};

        \draw[arrow] (p)  -- (a);
        \draw[arrow] (a)  -- (n);
        \draw[arrow] (n)  -- (m1);
        \draw[arrow] (m1) -- (m2);
        \draw[arrow] (m2) -- (m3);
        \draw[arrow] (m3) -- (m4);
        \draw[arrow] (m4) -- (d);
        \draw[arrow] (d)  -- node[right, font=\small]{是} (c);
        \draw[arrow] (d.west) -- ++(-1.2,0) |- node[pos=0.25, left, font=\small]{否} (core.west);
    \end{tikzpicture}
    \caption{本文整体求解框架}
    \label{fig:framework}
\end{figure}
