\section{问题一的建立与求解}

\subsection{模型建立}
基于前述假设，将问题一形式化为如下优化模型：
\begin{equation}
    \min_{x \in \mathbb{R}^n} f(x)
    \quad \text{s.t.} \quad
    g_i(x) \le 0,\; i = 1, \dots, m
    \label{eq:model1}
\end{equation}
其中 $f(x)$ 为目标函数，$g_i(x)$ 为约束条件。

\subsection{算法设计}
采用 XXXX 算法求解式~\eqref{eq:model1}，流程如算法~\ref{alg:solve1} 所示。

\begin{algorithm}[H]
    \caption{问题一求解算法}
    \label{alg:solve1}
    \KwIn{数据 $\mathcal{D}$、初始参数 $\theta_0$}
    \KwOut{最优解 $x^*$}
    初始化 $x \leftarrow x_0$\;
    \While{未收敛}{
        更新 $x$ 依据 XXXX 规则\;
        评估 $f(x)$\;
    }
    \Return{$x^* = x$}\;
\end{algorithm}

\subsection{结果与分析}
求解结果如表~\ref{tab:result1} 与图~\ref{fig:result1} 所示，结果表明 XXXX。

% \begin{figure}[H]
%     \centering
%     \includegraphics[width=0.7\textwidth]{result1.png}
%     \caption{问题一求解结果}
%     \label{fig:result1}
% \end{figure}
